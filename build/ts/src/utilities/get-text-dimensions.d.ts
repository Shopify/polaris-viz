export declare function getTextWidth({ text, fontSize, }: {
    text: string;
    fontSize: number;
}): number;
export declare function getTextHeight({ text, fontSize, containerWidth, }: {
    text: string;
    fontSize: number;
    containerWidth: number;
}): number;
//# sourceMappingURL=get-text-dimensions.d.ts.map