import {Meta} from '@storybook/addon-docs';

import {Title} from '../../../../polaris-viz/src/components/Docs/stories/components/Title/Title';
import {UtilitiesHeader} from '../../../../polaris-viz/src/components/Docs/stories/components/UtilitiesHeader/UtilitiesHeader';

<Meta
  title="Shared/Utilities/clamp"
  parameters={{
    viewMode: 'docs',
    docsOnly: true,
  }}
/>

<UtilitiesHeader />

<Title type="h3">
  <code>clamp()</code>
</Title>

<p>
  Is used to keep a number within the inclusive range within the min and max
  bounds. This method returns the clamped amount.
</p>

```tsx
clamp({
  amount: 10,
  min: 15,
  max: 20,
});

// Output:
// 15

clamp({
  amount: 1000,
  min: 150,
  max: 200,
});

// Output:
// 200
```
