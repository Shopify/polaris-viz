import {Meta} from '@storybook/addon-docs/blocks';
import LinkTo from '@storybook/addon-links/react';

import {PolarisVizProvider} from '@shopify/polaris-viz';
import {ComponentContainer, Title, SampleBarChart} from './components';

<Meta
  title="Shared/Themes/Default Themes"
  parameters={{
    viewMode: 'docs',
  }}
  component={PolarisVizProvider}
/>

<PolarisVizProvider>
<div style={{ margin: '0 auto', maxWidth: '800px', color: 'white'}}>

<Title>💅 Themes</Title>

<p>
  All Polaris Viz charts accept a <code>theme</code> prop that can be used to
  change the chart's appearance.
</p>
<p>
  Out of the box, the library comes with a <code>Default</code>, a{' '}
  <code>Light</code> and a <code>Print</code> theme:
</p>
<div
  style={{
    display: 'grid',
    gridGap: '20px',
    gridTemplateColumns: 'repeat(auto-fit, minmax(350px, 1fr))',
  }}
>
  <ComponentContainer
    title="Default Theme"
    description={
      <>
        If nothing is passed to the chart's <code>theme</code> prop, it
        will use the <code>Default</code> theme
      </>
    }
    theme="Default"
    chart={<SampleBarChart seriesLength={2} />}
    codeSample={'<BarChart />'}
  />
  <ComponentContainer
    title="Light Theme"
    description={
      <>
        The library also comes with a <code>Light</code> theme that can be
        applied to all charts
      </>
    }
    theme="Light"
    chart={<SampleBarChart seriesLength={2} theme="Light" />}
    codeSample={"<BarChart theme='Light'/>"}
  />
  <ComponentContainer
    title="Print Theme"
    description={
      <>
        Gets automatically applied to all charts when printing. Based on{' '}
        the <code>Light</code> theme but with reduced gradients to optimize for printing
      </>
    }
    theme="Light"
    chart={<SampleBarChart seriesLength={2} theme="Print" />}
    codeSample={"<BarChart theme='Print'/>"}
  />
</div>

<p>
  You can read more about customizing or creating your own themes in 👉
  <LinkTo kind='Shared/Themes/Customizing'>Theme customization page</LinkTo>.
</p>

<Title type="h2">🖨 Print Support</Title>
<p>We aim to make the components printable in all evergreen browsers.</p>
<p>
  However, Firefox{' '}
  <a target="_blank" href="https://bugzilla.mozilla.org/show_bug.cgi?id=774398">
    does not respond super well to print
  </a>
  . In fact, the print preview does not rerender the page. Even the <code>
    onbeforeprint
  </code> and <code>onafterprint</code> events do not fire in time. This can cause
  charts not to scale properly to the print page. For this reason, the print theme
  will also not work properly on Firefox.
</p>

</div>

</PolarisVizProvider>
