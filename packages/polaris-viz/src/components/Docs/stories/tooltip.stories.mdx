import {Meta, Story, Canvas} from '@storybook/addon-docs';
import {DEFAULT_THEME_NAME} from '@shopify/polaris-viz-core';

import {PolarisVizProvider} from '../../../';
import {Divider} from './components/Divider';
import {ComponentContainer} from './components/ComponentContainer/ComponentContainer';
import {Title} from './components/Title/Title';
import {ExamplesGrid} from './components/ExamplesGrid/ExamplesGrid';
import {PropertyTable} from './components/PropertyTable/PropertyTable';
import {
  SampleSparkbar,
  SampleSparkLineChart,
  SampleBarChart,
  SampleLineChart,
  SampleStackedAreaChart,
  SampleSimpleNormalizedChart,
} from './components/SampleComponents';

<Meta
  title="Shared/Themes/Theme Definition/Global Properties/tooltip"
  parameters={{
    viewMode: 'docs',
    docsOnly: true,
  }}
/>

<Title>✍️ Theme Definition</Title>

<Title type="h3">
  <code>tooltip</code>
</Title>

<PropertyTable global>
  <PropertyTable.Row
    property={`tooltip.backgroundColor`}
    type={'string'}
    description={<>A CSS color string that gets applied to the background</>}
  />
  <PropertyTable.Row
    property={`tooltip.valueColor`}
    type={'string'}
    description={<>A CSS color string that gets applied to values</>}
  />
</PropertyTable>

<Title type="h4">Examples:</Title>

<ExamplesGrid>

<ComponentContainer
  codeSample={`
    <PolarisVizProvider
      themes={{
        ${DEFAULT_THEME_NAME}: {
          tooltip: {
            backgroundColor: 'MidnightBlue',
            valueColor: 'yellow',
            labelColor: 'Fuchsia',
          },
        },
      }}
    >
    <StackedAreaCharts series={data} />
  </PolarisVizProvider>
    `}
  center
  chart={
    <div style={{width: '350px', height: '140px'}}>
      <PolarisVizProvider
        themes={{
          [DEFAULT_THEME_NAME]: {
            tooltip: {
              backgroundColor: 'MidnightBlue',
              valueColor: 'yellow',
              labelColor: 'Fuchsia',
            },
          },
        }}
      >
        <SampleBarChart />
      </PolarisVizProvider>
    </div>
  }
/>

</ExamplesGrid>
