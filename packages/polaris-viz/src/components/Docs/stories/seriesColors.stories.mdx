import {Meta, Story, Canvas} from '@storybook/addon-docs';
import {DEFAULT_THEME_NAME} from '@shopify/polaris-viz-core';

import {PolarisVizProvider} from '../../../';
import {ComponentContainer} from './components/ComponentContainer/ComponentContainer';
import {Title} from './components/Title/Title';
import {ExamplesGrid} from './components/ExamplesGrid/ExamplesGrid';
import {PropertyTable} from './components/PropertyTable/PropertyTable';
import {
  SampleSparkLineChart,
  SampleBarChart,
  SampleStackedAreaChart,
} from './components/SampleComponents';

<Meta
  title="Shared/Themes/Theme Definition/Global Properties/seriesColors"
  parameters={{
    viewMode: 'docs',
    docsOnly: true,
  }}
/>
;

<Title>✍️ Theme Definition</Title>

<Title type="h3">
  <code>seriesColors</code>
</Title>

`seriesColors` allows you to customize what colors the charts will use depending on how many data series are being displayed.

The `Dark` and `${DEFAULT_THEME_NAME}` themes that come with the library out of the box use gradients for charts with up to 7 data series, but use
solid colors for charts with more. The `Color` type accepts a gradient definition or any valid CSS string color:

```ts
export type Color = string | GradientStop[];

export interface GradientStop {
  offset: number;
  color: string;
}
```

<PropertyTable global>
  <PropertyTable.Row
    property={`seriesColors.single`}
    type={'Color'}
    description={
      <>Defines the color used in the series on charts with a single series</>
    }
  />
  <PropertyTable.Row
    property={`seriesColors.limited`}
    type={'Color[]'}
    description={
      <>
        Defines the colors used in the series on charts that have from 2 to 4
        series
      </>
    }
  />
  <PropertyTable.Row
    property={`seriesColors.all`}
    type={'Color[]'}
    description={
      <>
        Defines the colors used in the series on charts that have from 5 to 7
        series
      </>
    }
  />
  <PropertyTable.Row
    property={`seriesColors.all`}
    type={'Color[]'}
    description={
      <>
        Defines the colors used in the series on charts that have more than 7
        series
      </>
    }
  />
  <PropertyTable.Row
    property="seriesColors.comparison"
    type={'string'}
    description="CSS color string used when DataSeries.isComparison is true"
  />
</PropertyTable>

<Title type="h4">Examples:</Title>

<ExamplesGrid>

<ComponentContainer
  codeSample={`
    <PolarisVizProvider
      themes={{
        ${DEFAULT_THEME_NAME}: {
          seriesColors: {
            single: 'lime',
          },
        },
      }}
    >
    <StackedAreaCharts series={data} />
  </PolarisVizProvider>
    `}
  center
  chart={
    <div style={{width: '350px', height: '140px'}}>
      <PolarisVizProvider
        themes={{
          [DEFAULT_THEME_NAME]: {
            seriesColors: {
              single: 'lime',
            },
          },
        }}
      >
        <SampleStackedAreaChart seriesLength={1} />
      </PolarisVizProvider>
    </div>
  }
/>
<ComponentContainer
  codeSample={`
    <PolarisVizProvider
      themes={{
        ${DEFAULT_THEME_NAME}: {
          seriesColors: {
            limited: [
              'yellow',
              'lime',
              'aqua',
              'teal'
            ],
          },
        },
      }}
    >
    <MultiseriesBarChart series={data} />
  </PolarisVizProvider>
    `}
  center
  chart={
    <div style={{width: '350px', height: '140px'}}>
      <PolarisVizProvider
        themes={{
          [DEFAULT_THEME_NAME]: {
            seriesColors: {
              limited: ['yellow', 'lime', 'aqua', 'Teal'],
            },
          },
        }}
      >
        <SampleBarChart seriesLength={4} showLegend={false} />
      </PolarisVizProvider>
    </div>
  }
/>
<ComponentContainer
  codeSample={`
    <PolarisVizProvider
      themes={{
        ${DEFAULT_THEME_NAME}: {
          all: {
            all: [
              'magenta',
              'orange',
              'DarkOrchid',
              'yellow',
              'lime',
              'aqua',
              'Teal'
            ],
          },
        },
      }}
    >
    <MultiseriesBarChart series={data} />
  </PolarisVizProvider>
    `}
  center
  chart={
    <div style={{width: '350px', height: '140px'}}>
      <PolarisVizProvider
        themes={{
          [DEFAULT_THEME_NAME]: {
            seriesColors: {
              all: [
                'magenta',
                'orange',
                'DarkOrchid',
                'yellow',
                'lime',
                'aqua',
                'Teal',
              ],
            },
          },
        }}
      >
        <SampleBarChart seriesLength={7} showLegend={false} />
      </PolarisVizProvider>
    </div>
  }
/>

<ComponentContainer
  codeSample={`
    <PolarisVizProvider
      themes={{
        ${DEFAULT_THEME_NAME}: {
          all: {
            all: [
              'GreenYellow',
              'ForestGreen',
              'LawnGreen',
              'Lime',
              'LimeGreen',
              'PaleGreen',
              'YellowGreen',
              'MediumSpringGreen',
              'SpringGreen',
              'MediumSeaGreen',
              'SeaGreen',
            ],
          },
        },
      }}
    >
    <MultiseriesBarChart series={data} />
  </PolarisVizProvider>
    `}
  center
  chart={
    <div style={{width: '350px', height: '140px'}}>
      <PolarisVizProvider
        themes={{
          [DEFAULT_THEME_NAME]: {
            seriesColors: {
              all: [
                'GreenYellow',
                'ForestGreen',
                'LawnGreen',
                'Lime',
                'LimeGreen',
                'PaleGreen',
                'YellowGreen',
                'MediumSpringGreen',
                'SpringGreen',
                'MediumSeaGreen',
                'SeaGreen',
              ],
            },
          },
        }}
      >
        <SampleBarChart seriesLength={11} showLegend={false} />
      </PolarisVizProvider>
    </div>
  }
/>

<ComponentContainer
  codeSample={`
    <PolarisVizProvider
      themes={{
        ${DEFAULT_THEME_NAME}: {
          seriesColors: {
            comparison: 'lime',
          },
        }
      }}
    >
    <SparkLineChart series={data} />
  </PolarisVizProvider>
    `}
  center
  chart={
    <div style={{width: '350px', height: '140px'}}>
      <PolarisVizProvider
        themes={{
          [DEFAULT_THEME_NAME]: {
            seriesColors: {
              comparison: 'lime',
            },
          },
        }}
      >
        <SampleSparkLineChart />
      </PolarisVizProvider>
    </div>
  }
/>

</ExamplesGrid>
