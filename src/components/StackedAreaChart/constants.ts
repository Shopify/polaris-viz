export const MIN_Y_LABEL_SPACE = 80;
export const AREA_OPACITY = 0.8;

export enum Spacing {
  Tight = 8,
  Loose = 20,
  ExtraTight = 4,
}

export enum Margin {
  Top = Spacing.Tight,
  Left = 0,
  Bottom = 30,
  Right = Spacing.ExtraTight,
}
