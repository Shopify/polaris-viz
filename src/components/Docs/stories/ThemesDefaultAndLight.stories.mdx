import {Meta} from '@storybook/addon-docs/blocks';

import {PolarisVizProvider} from '../../PolarisVizProvider';
import {ComponentContainer, Title, SampleSparkline} from './components';

<Meta
  title="Docs/Themes/Default and Light"
  parameters={{
    viewMode: 'docs',
  }}
  component={PolarisVizProvider}
/>

<PolarisVizProvider
    themes={{
      Default: {
        chartContainer: {
          padding: '20px',
        }
      },
      Light: {
        chartContainer: {
          padding: '20px',
        }
      },
    }}>
<div style={{ margin: '0 auto', maxWidth: '800px', color: 'white'}}>

<Title>💅 Themes</Title>

<p>
  All Polaris Viz charts accept a <code>theme</code> prop that can be used to
  change the chart's appearence.
</p>
<p>
  Out of the box, the library comes with a <code>Default</code> and a{' '}
  <code>Light</code> themes:
</p>
<div
  style={{
    display: 'grid',
    gridGap: '20px',
    gridTemplateColumns: 'repeat(auto-fit, minmax(250px, 1fr))',
  }}
>
  <ComponentContainer
    title="Default Theme"
    description={
      <>
        If nothing is passed to the <code>theme</code> prop of the chart, it
        will use the <code>Default</code> theme
      </>
    }
    theme="Default"
    center
    chart={
      <div style={{width: '250px', height: '140px'}}>
        <SampleSparkline />
      </div>
    }
    codeSample={'<Sparkline />'}
  />
  <ComponentContainer
    title="Light Theme"
    description={
      <>
        The library also comes with a <code>Light</code> theme that can be
        applied to all charts
      </>
    }
    theme="Light"
    center
    chart={
      <div style={{width: '250px', height: '140px'}}>
        <SampleSparkline theme="Light" />
      </div>
    }
    codeSample={"<Sparkline theme='Light'/>"}
  />
</div>

<p>
  You can read more about customizing or creating your own themes in the
  <a href="/docs/docs-themes-customizing--page"> 👉 Theme customization page</a>
</p>

</div>

</PolarisVizProvider>
